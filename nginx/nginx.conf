server {
    listen 8080;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri.html /index.html;
    }

    location /healthz {
        access_log off;
        return 200;
    }

    location ^~ /app {
        try_files $uri /app.html;
    }
}
